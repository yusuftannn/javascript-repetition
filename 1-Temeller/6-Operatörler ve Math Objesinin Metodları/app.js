let value;

const value1 = 10

const value2 = 4

// Aritmetik operatörler

// value = value1 + value2;
// value = value1 - value2;
// value = value1 * value2;
// value = value1 / value2;
// value = value1 % value2;


// ***** Math operatörleri ***** //
value = Math.PI
value = Math.E

value = Math.round(3.6) // yukarı yuvarlar
value = Math.round(3.5) // yukarı yuvarlar
value = Math.round(3.3) // aşağı yuvarlar

value = Math.ceil(3.2)  // tüm ondalıklı sayıları yukarı yuvarlar.
value = Math.floor(3.8) // tüm ondalıklı sayıları aşağı yuvarlar.

value = Math.max(10,-1,100,32)  // aralarından en büyüğü ekrana yazdır
value = Math.min(10,-1,100,32)  // aralarından en küçüğü ekrana yazdır

value = Math.random()
value = Math.random() * 20  // 1den 20ye kadar random sayılar üretir.
console.log(value);